name: Garmin Morning Sync

on:
  schedule:
    # Runs everyday at 06:30 AM UTC (Adjust to your local timezone preference)
    - cron: '30 6 * * *'
  workflow_dispatch: # Allows you to force run it manually with a button click

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        pip install python-garminconnect

    - name: Run Fetch Script
      env:
        GARMIN_EMAIL: ${{ secrets.GARMIN_EMAIL }}
        GARMIN_PASSWORD: ${{ secrets.GARMIN_PASSWORD }}
      run: python fetch_garmin.py

    - name: Commit and Push Updates
      run: |
        git config --global user.name "Garmin Sync Bot"
        git config --global user.email "actions@github.com"
        git add sleep_database.json
        git diff-index --quiet HEAD || git commit -m "Automated Morning Garmin Update [skip ci]"
        git push
